# Dependencias (se vuelven a instalar dentro del contenedor)
node_modules
npm-debug.log
yarn-debug.log
yarn-error.log

# Directorios de build locales (Docker genera el suyo propio)
build
dist

# Archivos de configuración del sistema / IDE
.DS_Store
.vscode
.idea
*.swp

# Git
.git
.gitignore

# Env files locales (se pasan como secretos/variables en Cloud Run)
# .env
# .env.local
# .env.development.local
# .env.test.local
# .env.production.local
